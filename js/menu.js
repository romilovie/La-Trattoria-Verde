// =====================================================
// МОДУЛЬ ФИЛЬТРАЦИИ МЕНЮ (menu.js)
// =====================================================

// Получение элементов управления фильтрацией и карточек меню
const buttons = document.querySelectorAll('.filter-btn');
const cards = document.querySelectorAll('.menu-card');

// -----------------------------------------------------
// ПРОВЕРКА НАЛИЧИЯ НЕОБХОДИМЫХ ЭЛЕМЕНТОВ В DOM
// -----------------------------------------------------
if (buttons.length > 0 && cards.length > 0) {

    // -------------------------------------------------
    // НАЗНАЧЕНИЕ ОБРАБОТЧИКОВ СОБЫТИЙ ДЛЯ КНОПОК ФИЛЬТРА
    // -------------------------------------------------
    buttons.forEach(btn => {
        btn.addEventListener('click', () => {

            // Сброс активного состояния у всех кнопок
            buttons.forEach(b => b.classList.remove('active'));

            // Установка активного состояния текущей кнопки
            btn.classList.add('active');

            // Получение значения фильтра из атрибута data-filter
            const filter = btn.dataset.filter;

            // ---------------------------------------------
            // ФИЛЬТРАЦИЯ КАРТОЧЕК МЕНЮ
            // ---------------------------------------------
            cards.forEach(card => {

                // Получение списка тегов карточки
                const tags = card.dataset.tags.split(' ');

                // Проверка соответствия карточки выбранному фильтру
                if (filter === 'all' || tags.includes(filter)) {
                    card.style.display = 'block';   // Отобразить карточку
                } else {
                    card.style.display = 'none';    // Скрыть карточку
                }
            });
        });
    });
}
}
